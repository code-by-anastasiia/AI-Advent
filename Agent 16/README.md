Document Indexing System | Система индексации документов

День 16 из курса по AI агентам - создание локального индекса документов с векторными эмбеддингами

О проекте
Это реализация пайплайна для индексации текстовых документов с использованием векторных эмбеддингов. Система разбивает документы на чанки, создаёт семантические эмбеддинги и сохраняет индекс для быстрого семантического поиска.
Основа для RAG (Retrieval-Augmented Generation) систем.

Возможности
Поддержка множества форматов: .txt, .md, .py, .js, .html, .css, .json
Умная разбивка текста на чанки с перекрытием
Генерация векторных эмбеддингов с помощью Sentence Transformers
Сохранение индекса в JSON формате
Семантический поиск по проиндексированным документам
Поддержка русского и английского языков

Быстрый старт
Установка зависимостей
bashpip install sentence-transformers
Использование
bash# Запустить индексацию
python index_real_documents.py
Программа автоматически:

Создаст папку documents/ с примерами (если её нет)
Прочитает все текстовые файлы
Разобьёт их на чанки
Создаст эмбеддинги
Сохранит индекс в document_index.json
Протестирует поиск

Структура проекта
1. Загрузка документов
pythondocuments = load_documents_from_folder("documents/")
2. Разбивка на чанки
Текст разбивается на фрагменты ~300 символов с перекрытием 50 символов:
Документ → [Чанк 1] [Чанк 2] [Чанк 3] ...
              ↓         ↓         ↓
           overlap   overlap   overlap
3. Создание эмбеддингов
Каждый чанк превращается в вектор из 384 чисел:
pythonmodel = SentenceTransformer("paraphrase-multilingual-MiniLM-L12-v2")
embeddings = model.encode(chunks)
4. Семантический поиск
pythonquery = "Что такое машинное обучение?"
# Находит релевантные фрагменты по смыслу, а не по точному совпадению слов
results = search(query, top_k=3)

Пример результата
- После индексации 5 документов:
- Загружено 5 документов
- Создано 52 чанка
- Сгенерировано 52 эмбеддинга (по 384 измерения каждый)
- Индекс сохранён: document_index.json (280 KB)

Технологии
Python 3.8+
Sentence Transformers - создание эмбеддингов
NumPy - векторные вычисления
JSON - хранение индекса

Откройте Pull Request

📝 Лицензия
